<Page x:Class="ClinicManagementSystem.Pages.InvoicesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      FlowDirection="RightToLeft"
      Title="Invoices">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="💰 إدارة الفواتير" FontSize="24" FontWeight="Bold" Foreground="#333"/>
                    <TextBlock Text="عرض وإدارة جميع الفواتير والمدفوعات" FontSize="14" Foreground="#666" Margin="0,5,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" Content="➕ فاتورة جديدة" Background="#4CAF50" Foreground="White" 
                        BorderThickness="0" Padding="20,10" FontSize="14" Cursor="Hand" Click="NewInvoice_Click"/>
            </Grid>
        </Border>

        <!-- Filters & Stats -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Stats Cards -->
                <UniformGrid Grid.Row="0" Columns="4" Margin="0,0,0,15">
                    <Border Background="#E3F2FD" CornerRadius="8" Padding="15" Margin="5">
                        <StackPanel>
                            <TextBlock Text="💵 إجمالي المدفوع" FontSize="12" Foreground="#1976D2"/>
                            <TextBlock x:Name="txtTotalPaid" Text="0 جنيه" FontSize="20" FontWeight="Bold" Foreground="#1976D2" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#FFF3E0" CornerRadius="8" Padding="15" Margin="5">
                        <StackPanel>
                            <TextBlock Text="⏳ إجمالي المتبقي" FontSize="12" Foreground="#F57C00"/>
                            <TextBlock x:Name="txtTotalRemaining" Text="0 جنيه" FontSize="20" FontWeight="Bold" Foreground="#F57C00" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#FFEBEE" CornerRadius="8" Padding="15" Margin="5">
                        <StackPanel>
                            <TextBlock Text="⚠️ فواتير غير مدفوعة" FontSize="12" Foreground="#D32F2F"/>
                            <TextBlock x:Name="txtUnpaidCount" Text="0" FontSize="20" FontWeight="Bold" Foreground="#D32F2F" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#E8F5E9" CornerRadius="8" Padding="15" Margin="5">
                        <StackPanel>
                            <TextBlock Text="✅ فواتير مدفوعة" FontSize="12" Foreground="#388E3C"/>
                            <TextBlock x:Name="txtPaidCount" Text="0" FontSize="20" FontWeight="Bold" Foreground="#388E3C" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- Search & Filters -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="txtSearch" Grid.Column="0" FontSize="14" Padding="10" 
                             VerticalContentAlignment="Center" TextChanged="txtSearch_TextChanged">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="BorderBrush" Value="#BDBDBD"/>
                                <Setter Property="BorderThickness" Value="1"/>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <ComboBox x:Name="cmbStatusFilter" Grid.Column="1" Width="150" Margin="10,0" 
                              FontSize="14" SelectionChanged="StatusFilter_Changed">
                        <ComboBoxItem Content="جميع الفواتير" IsSelected="True"/>
                        <ComboBoxItem Content="مدفوع"/>
                        <ComboBoxItem Content="غير مدفوع"/>
                        <ComboBoxItem Content="مدفوع جزئياً"/>
                    </ComboBox>

                    <Button Grid.Column="2" Content="🔍 بحث متقدم" Background="#FF9800" Foreground="White" 
                            BorderThickness="0" Padding="15,10" Margin="5,0" FontSize="14" Cursor="Hand" 
                            Click="AdvancedSearch_Click"/>

                    <Button Grid.Column="3" Content="📊 تصدير Excel" Background="#4CAF50" Foreground="White" 
                            BorderThickness="0" Padding="15,10" FontSize="14" Cursor="Hand" Click="ExportExcel_Click"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Invoices DataGrid -->
        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <TextBlock Text="إجمالي الفواتير: " FontSize="14" Foreground="#666" VerticalAlignment="Center"/>
                    <TextBlock x:Name="txtTotalCount" Text="0" FontSize="14" FontWeight="Bold" 
                               Foreground="#2196F3" VerticalAlignment="Center" Margin="5,0,0,0"/>
                </StackPanel>

                <DataGrid x:Name="dgInvoices" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                          GridLinesVisibility="None" HeadersVisibility="Column" Background="White"
                          RowBackground="#FAFAFA" AlternatingRowBackground="White" BorderThickness="0"
                          CanUserResizeColumns="True" SelectionMode="Single" 
                          MouseDoubleClick="dgInvoices_MouseDoubleClick">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F5F5F5"/>
                            <Setter Property="Foreground" Value="#333"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="BorderThickness" Value="0,0,0,2"/>
                            <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E3F2FD"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="رقم الفاتورة" Binding="{Binding InvoiceNumber}" Width="120"/>
                        <DataGridTextColumn Header="التاريخ" Binding="{Binding InvoiceDate, StringFormat='{}{0:dd/MM/yyyy}'}" Width="100"/>
                        <DataGridTextColumn Header="المريض" Binding="{Binding PatientName}" Width="*"/>
                        <DataGridTextColumn Header="النوع" Binding="{Binding InvoiceType}" Width="100"/>
                        <DataGridTextColumn Header="الإجمالي" Binding="{Binding TotalAmount, StringFormat='{}{0:N2}'}" Width="100"/>
                        <DataGridTextColumn Header="الخصم" Binding="{Binding DiscountAmount, StringFormat='{}{0:N2}'}" Width="80"/>
                        <DataGridTextColumn Header="الصافي" Binding="{Binding NetAmount, StringFormat='{}{0:N2}'}" Width="100"/>
                        <DataGridTextColumn Header="المدفوع" Binding="{Binding PaidAmount, StringFormat='{}{0:N2}'}" Width="100"/>
                        <DataGridTextColumn Header="المتبقي" Binding="{Binding RemainingAmount, StringFormat='{}{0:N2}'}" Width="100"/>

                        <DataGridTemplateColumn Header="الحالة" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="8,4">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding PaymentStatus}" Value="مدفوع">
                                                        <Setter Property="Background" Value="#C8E6C9"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding PaymentStatus}" Value="غير مدفوع">
                                                        <Setter Property="Background" Value="#FFCDD2"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding PaymentStatus}" Value="مدفوع جزئياً">
                                                        <Setter Property="Background" Value="#FFE082"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding PaymentStatus}" FontSize="11" 
                                                   FontWeight="Bold" HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="الإجراءات" Width="250">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="👁️ عرض" Background="#2196F3" Foreground="White"
                                                BorderThickness="0" Padding="8,4" Margin="2" FontSize="12"
                                                Cursor="Hand" Click="ViewInvoice_Click" Tag="{Binding InvoiceID}"/>

                                        <Button Content="💰 دفعة" Background="#4CAF50" Foreground="White"
                                                BorderThickness="0" Padding="8,4" Margin="2" FontSize="12"
                                                Cursor="Hand" Click="AddPayment_Click" Tag="{Binding InvoiceID}"/>

                                        <Button Content="🖨️ طباعة" Background="#FF9800" Foreground="White"
                                                BorderThickness="0" Padding="8,4" Margin="2" FontSize="12"
                                                Cursor="Hand" Click="PrintInvoice_Click" Tag="{Binding InvoiceID}"/>

                                        <Button Content="🗑️ حذف" Background="#F44336" Foreground="White"
                                                BorderThickness="0" Padding="8,4" Margin="2" FontSize="12"
                                                Cursor="Hand" Click="DeleteInvoice_Click" Tag="{Binding InvoiceID}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Pagination -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Button Content="◄◄ الأولى" Background="#E0E0E0" Foreground="#333" BorderThickness="0"
                            Padding="10,5" Margin="5" Click="FirstPage_Click"/>
                    <Button Content="◄ السابقة" Background="#E0E0E0" Foreground="#333" BorderThickness="0"
                            Padding="10,5" Margin="5" Click="PreviousPage_Click"/>
                    <TextBlock x:Name="txtPageInfo" Text="صفحة 1 من 1" VerticalAlignment="Center"
                               Margin="10,0" FontSize="14"/>
                    <Button Content="التالية ►" Background="#E0E0E0" Foreground="#333" BorderThickness="0"
                            Padding="10,5" Margin="5" Click="NextPage_Click"/>
                    <Button Content="الأخيرة ►►" Background="#E0E0E0" Foreground="#333" BorderThickness="0"
                            Padding="10,5" Margin="5" Click="LastPage_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>